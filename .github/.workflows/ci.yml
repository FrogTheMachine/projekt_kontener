name: projekt_kontener CI Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test-container-engine:    
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Pobranie kodu repozytorium
      uses: actions/checkout@v4

    - name: ⚙️ Weryfikacja środowiska (Python & Make)
      run: |
        python3 --version
        make --version

    - name: 🚀 Prowizjonowanie środowiska (make prepare)
      run: make prepare

    - name: 🧪 Test automatyczny izolacji i sieci (make test)      
      run: make test